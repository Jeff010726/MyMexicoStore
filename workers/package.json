{
  "name": "ecommerce-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for ecommerce site",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test": "vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0",
    "wrangler": "^3.28.0"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "jose": "^5.2.0",
    "zod": "^3.22.4"
  }
}